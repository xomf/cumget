# cumget
command line download utility with a simple interface

building: `g++ main.cpp -o cumget -lcurl`

example 1: `cumget https://example.com/`

example 2: `cumget https://example.com/ index.html`
